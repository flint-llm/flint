apiVersion: v1
kind: Service
metadata:
  name: {{ DATASPINE_IMAGE_REGISTRY_NAMESPACE }}-{{ DATASPINE_MODEL_NAME }}-nodeport
  labels:
    app: {{ DATASPINE_IMAGE_REGISTRY_NAMESPACE }}-{{ DATASPINE_MODEL_NAME }} 
    hystrix.enabled: "true"
spec:
  type: NodePort
  ports:
  - name: http-nginx
    port: 8080
    targetPort: 8080
    nodePort: 31400
  selector:
    app: {{ DATASPINE_IMAGE_REGISTRY_NAMESPACE }}-{{ DATASPINE_MODEL_NAME }}
